__pycache__/
*.pyc
.venv/
.env
*.egg-info/
dist/
build/
.pytest_cache/
*.log
# Local MonitorX operations history and configuration
monitorx-operations.db
monitorx-operations.db-shm
monitorx-operations.db-wal
vm-insights-config.json

# Node tooling (jsdom smoke tests). Keep package-lock.json tracked so CI and
# local smoke tests use the same dependency graph.
node_modules/

# MonitorX local state directory (MONITORX_STATE_DIR)
.state/
monitorx-audit.log
